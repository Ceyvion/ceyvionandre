/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/ceyvionandre',
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
}

export default nextConfig
